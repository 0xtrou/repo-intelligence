node_modules/
*.log
.DS_Store

# Per-target working data stays local — it must not pollute the shared toolkit repo.
# runs/ is append-only measurement history on the machine that measures (never edited,
# never committed); plans/ are per-target MeasurementPlans.
runs/
plans/
